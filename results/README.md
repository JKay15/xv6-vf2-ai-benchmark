# results

This directory is reserved for benchmark outputs:

- build logs
- deploy logs
- serial logs
- U-Boot transcripts
- recovery logs
- score sheets

Store each benchmark attempt in its own subdirectory.

