# agents

This directory is reserved for per-agent metadata, such as:

- agent name
- model version
- prompt contract used
- branch or patch identifiers
- run summaries

Keep raw logs in `results/`, not here.

